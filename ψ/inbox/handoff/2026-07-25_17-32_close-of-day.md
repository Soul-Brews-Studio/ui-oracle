# Handoff: close of day — globe perf shipped, cross-repo review marathon

**Date**: 2026-07-25 17:32
**Source**: /forward (interactive)
📡 Session: 8549f552 | ui-oracle | 2026-07-11 → 2026-07-25 (long multi-day session)

## What We Did

- **Globe perf shipped** (PR #103, merged, deployed): `/map` (`apps/studio/src/pages/Map.tsx`) and `/planets` (vendored `knowledge-map-3d` patch) converted per-doc `THREE.Mesh` → `InstancedMesh`; hover stutter fixed by moving the tooltip off React state to direct DOM.
- **Build footer + Disconnect + cache fix** — `BuildFooter` (version + GMT date) on all 7 apps; `ui`/`api` version labels; real offline `Disconnect` state in `BackendGate`; host-scoped client cache keys (muninn's bug).
- **MCP re-wired** — `arra-oracle` re-registered as **stdio** in `~/.claude.json` (HTTP `/mcp` is bearer-gated); tool surface trimmed to 14 via `~/.arra-oracle-v2/config.json`; global `~/.claude/CLAUDE.md` `muninn_search` → `oracle_search` (that tool never existed).
- **Cross-repo review for arra-oracle-v3** (review/nudge only, never wrote to their repo): drafted the plugin-surface conformance test (v1→v3, landed as their #2828); reviewed #2836 (/api/learn N+1 hang) and #2838 (Thai-slug data loss) by re-deriving numbers; closed #2831; nudged #2819/#2806.
- **Deployed all 8 apps** at `v0.1.0+5255c06`, prod-verified every domain against HEAD.
- **PRs merged this repo**: #103 (globe), #104 (session vault), #105 (corrected handoff).

## Pending

- [ ] Capture real before/after `/map` fps and attach to PR #103 — blocked all session on Chrome MCP being down
- [ ] Version `~/.claude/CLAUDE.md` — the muninn_search fix is live but untracked, one `rm` from gone (needs a careful `.gitignore` first — that dir holds credentials/transcripts)
- [ ] Add `rg --version` preflight to the CLAUDE.md search-discipline rule (rule fails silently where `rg` is shadowed by BSD grep)

## Their repo (arra-oracle-v3) — nudged, not ours to fix

- [ ] #2819 Thai-slug data loss — fix merged (#2838), verify it lands on alpha; watch for the Unicode-slug follow-up (files named `learning-2/-3` carry no meaning)
- [ ] #2806 empty-embedding indexer — still OPEN, dormant (daemon not running)
- [ ] **/api/memory/consolidation/suggestions self-hangs ~11 min** — the real blocker, found by arra-oracle-v3 using the queue-time filter, reproduced deterministically by me in a clean process (686,729ms). 2nd self-hanging endpoint of the day after /api/learn (#2836). `/api/stats` + `/api/health` are VICTIMS — they queue behind it on the single loop (I re-wedged the backend just by calling the consolidation endpoint once). Their fix incoming: (1) fix consolidation endpoint [primary], (2) stats TTL cache [defense-in-depth]. Also flagged: `withTimeout` at `vector-operations.ts:77` is `Promise.race`+`setTimeout` = blind to sync blocks (decorative timeout). Their repo; review offered.
  - Note: any explanation that lets you STOP looking ("can't reproduce, add cache, move on") deserves extra suspicion — same close-early shape as the #2836 "it was just agent contention" near-miss. Both nearly closed this; both were wrong.

## Cross-repo answered (no action)

- v4.buildwithoracle.com + localhost:47778/ serve arra-oracle-v3's OWN frontend (Memory dashboard, reads /api/memory/recall = 1 entry), NOT ui-oracle studio. Different app, different data source. If Nat wants v4 to show full studio → nudge arra-oracle-v3 (their frontend routing).

## Cleanup (stale, pre-existing — not from this session)

- [ ] PR #88 `fix(map3d): don't cache empty results (closes #87)` — review/merge or close
- [ ] PR #84 `Align studio canvas split and tests` — review/merge or close
- [ ] PR #81 `feat(apps/pulse): status page SPA` — review/merge or close

## Key Files

- `apps/studio/src/pages/Map.tsx` — the /map globe (InstancedMesh + GPU shader)
- `packages/shared-ui/src/components/BuildFooter.tsx`, `.../AppHeader/HostPicker.tsx`, `apps/studio/src/components/BackendGate.tsx`
- `~/.arra-oracle-v2/config.json` — MCP tool whitelist (config, not repo)
- `/tmp/plugin-surface-conformance.test.ts` — the draft sent to arra-oracle-v3 (landed as #2828)
- `ψ/memory/learnings/2026-07-25_reproduce-dont-arbitrate.md` + `..._verify-the-target-before-fixing-it.md` — the two lessons of the day

## Session vitals

- Data loss risk: #2819 (Thai learnings) fixed upstream this session
- Escalation flagged in retro: browser-verification unavailable in 4/4 metrics rows → root-cause candidate (headless Playwright harness in-repo)
