# Handoff: globe perf shipped · MCP re-wired · cross-repo bug hunt in arra-oracle-v3

**Date**: 2026-07-25 16:59
**Source**: /forward-bg (JSONL-mined), then **corrected against live state** — the
first auto-generated draft had 3 claims that did not survive verification. See
"Corrections" at the bottom; they are instructive, not just bookkeeping.
📡 Session: 8549f552 | ui-oracle | spans 2026-07-11 → 2026-07-25

## What We Did

**ui-oracle (this repo) — all merged and deployed**
- `/map` globe: per-doc `THREE.Mesh` → `InstancedMesh` (~35k draw calls → 1/globe).
  The laggy page was **`apps/studio/src/pages/Map.tsx`**, studio's own renderer.
- `/planets` globe: same conversion in the vendored `knowledge-map-3d` package
  (via `bun patch`; the patch had to be regenerated by hand — `bun patch --commit`
  segfaults on 1.3.14).
- Hover stutter took 3 more rounds: GPU-shader jitter/magnify → bounded top-8
  label collection → **the actual cause was a React-state tooltip re-rendering the
  whole ~1000-line page on every hover.** Now direct-DOM.
- `BuildFooter` (version + GMT build date) across all 7 apps; `ui`/`api` version
  labels in the header; **Disconnect** button with a real offline state in
  `BackendGate`; host-scoped client cache keys (bug reported by muninn).
- **PR #103** (globe perf) and **PR #104** (session vault) merged; studio +
  all-app + canvas deployed and verified in the live bundle.

**MCP / tooling**
- Re-registered `arra-oracle` MCP as a **stdio** server in `~/.claude.json`
  (HTTP `/mcp` is bearer-gated and was unusable), proxying to `:47778`.
- Trimmed the tool surface via `~/.arra-oracle-v2/config.json`.
- Fixed `~/.claude/CLAUDE.md`: it told every session to call
  `mcp__arra-oracle__muninn_search`, **a tool that does not exist**. Now
  `oracle_search`, plus notes on the trimmed surface and dead alias prefixes.

**arra-oracle-v3 (THEIR repo — reviewed/nudged, never written to)**
- Found and reported: `toolSettingsRoute` defined but never mounted (404);
  `ORACLE_DISABLED_TOOLS` read but never applied; `.arra/config.json` written by
  the API but never read (wrong filename); `mcpTools[].group` controls nothing;
  19 of 40 `tool:` fields naming MCP tools that do not exist; 14 verbs on
  CLI+API but not MCP.
- Drafted the **plugin surface conformance suite** (v1→v3 across two review
  rounds). Landed as **#2828**.
- Spotted the backend pinned at **95% CPU**; it was `GET /api/learn` —
  unbounded SELECT + N+1 over an FTS5 table with `id UNINDEXED`. Reviewed
  **#2836**; merged; **verified live: hang → 200 in 57ms**.
- Closed **#2831** (CLAUDE.md claimed the backend served `frontend/dist`;
  fixed by #2832, verified by probing the running server).

## Uncommitted Files

```
?? ψ/inbox/handoff/2026-07-25_16-59_bg-forward.md   (this file)
```
Everything else is committed. PR #104 (`b8926a3`) merged the retro, lesson,
metrics row, and the 12:36 handoff.

## Pending

**Theirs — nudged, both still OPEN, neither started**
- **#2819** `arra_learn` slug regex strips non-ASCII → Thai learnings silently
  lost. **Highest priority: this is live data loss** on any day two Thai
  learnings are recorded. The fix already exists in their repo as dead code
  (`src/learn/markdown.ts:34`) but needs a collision suffix alongside it.
- **#2806** indexer writes `document: ''` and discards the real vector.
  Dormant — the daemon is not running — but `startDaemon()` enters the path
  unconditionally. Already-poisoned rows need re-embedding, not just a code fix.
- `/mcp` and `/simple` behaviour still unverified for the 406 doc line.

**Ours**
- `~/.claude/CLAUDE.md` is **not in any git repo** — today's fix is one `rm`
  from gone. Versioning it needs a careful `.gitignore` first (that directory
  holds credentials and transcripts).
- Never added the `rg --version` preflight to CLAUDE.md. Worth it: the
  "rg only, never filesystem sweeps" rule **fails silently** wherever `rg` is
  shadowed by BSD grep — which happened to arra-oracle-v3 today.
- Real before/after fps for PR #103 were never captured (no browser
  automation). The draw-call reduction is proven; the fps number is not.

## Next Session

1. Check whether arra-oracle-v3 picked up **#2819** — offer to draft the patch
   for their review if it is still untouched.
2. Decide on versioning `~/.claude/CLAUDE.md`, and add the `rg` preflight.
3. If a browser is available, capture the `/map` fps figure and attach it to #103.

## Corrections to the auto-generated draft

Recorded because the failure mode is the session's own recurring theme —
*something declared that nothing checked*:

| Draft claimed | Reality |
|---|---|
| 4 uncommitted `ψ/` files | Clean — merged in PR #104. Stale by ~4 hours. |
| "#2836 = CLAUDE.md sync" | #2836 is `fix(learn): GET /api/learn wedged the server`. Conflated with #2832/the 406 thread. |
| "progressed on #2806 fixes" | Not started. Still OPEN. Only nudged. |
| "confirm deploy to god.buildwithoracle.com" | That is **maw-ui's** deploy, unrelated to #2836 or this repo. |
| "15 failing manifest cases, scheduled rework" | Not defects — manifests are simply un-annotated. The suite ships in `warn` mode for exactly that reason. |
| globe fix was "apps/canvas PlanetsCanvas" | The real fix was `apps/studio/src/pages/Map.tsx`. `PlanetsCanvas` was the **wrong target chased first** — the mistake recorded in `2026-07-25_verify-the-target-before-fixing-it.md`. |
